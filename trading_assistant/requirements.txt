# ============================================================
# Indian Stock Market Intraday Trading Assistant — Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# GPU note: torch ships with CUDA support on pip by default.
#           For CPU-only, use: pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Core Data ---
pandas>=2.0.0
numpy>=1.24.0
pytz

# --- Stock Market Data ---
yfinance>=0.2.31

# --- RSS / News Scraping ---
feedparser>=6.0.10

# --- Technical Analysis ---
# Indicators (RSI, MACD, BB, VWAP, EMA, ATR) implemented with pure
# pandas/numpy — no pandas-ta needed, avoids numba/Python 3.14 issues.

# --- Sentiment Analysis ---
transformers>=4.35.0
torch>=2.1.0
textblob>=0.17.1

# --- ML Models ---
scikit-learn>=1.3.0
xgboost>=2.0.0
# NOTE: LSTM uses PyTorch (torch) instead of TensorFlow.
# Single DL backend — torch is already required by transformers/FinBERT.

# --- Backtesting ---
backtesting>=0.3.3

# --- Dashboard ---
streamlit>=1.28.0
plotly>=5.18.0

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Scheduling & Utilities ---
apscheduler>=3.10.4
tenacity>=8.2.3
joblib>=1.3.2
